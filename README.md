# voidwake-site

Static export of the Voidwake Studios marketing site for Namecheap Stellar (`public_html`).

The deployable archive is a GitHub Release asset:

`https://github.com/GabeTC99/voidwake-site/releases/download/static-v1/voidwake-static.zip`

Unzip so `index.html` is in the document root.
